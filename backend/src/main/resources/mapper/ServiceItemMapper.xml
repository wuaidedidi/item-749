<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.laundry.mapper.ServiceItemMapper">

    <resultMap id="ServiceItemResultMap" type="com.laundry.entity.ServiceItem">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="category" column="category"/>
        <result property="price" column="price"/>
        <result property="unit" column="unit"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="ServiceItemResultMap">
        SELECT * FROM service_item WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="ServiceItemResultMap">
        SELECT * FROM service_item
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') 
                     OR description LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY category, name
        LIMIT #{offset}, #{limit}
    </select>

    <select id="findAllActive" resultMap="ServiceItemResultMap">
        SELECT * FROM service_item WHERE status = 1 ORDER BY category, name
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM service_item
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') 
                     OR description LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <insert id="insert" parameterType="com.laundry.entity.ServiceItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO service_item (name, category, price, unit, description, status)
        VALUES (#{name}, #{category}, #{price}, #{unit}, #{description}, #{status})
    </insert>

    <update id="update" parameterType="com.laundry.entity.ServiceItem">
        UPDATE service_item
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="category != null">category = #{category},</if>
            <if test="price != null">price = #{price},</if>
            <if test="unit != null">unit = #{unit},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE service_item SET status = #{status} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM service_item WHERE id = #{id}
    </delete>

    <select id="findAllCategories" resultType="string">
        SELECT DISTINCT category FROM service_item ORDER BY category
    </select>

</mapper>
